{
  "name": "joma-order-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "npx playwright install chromium --with-deps && node index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.42.0"
  }
}
